# Backend configuration for TEST environment
bucket         = "your-terraform-state-bucket"
key            = "chatbot-service/test/terraform.tfstate"
region         = "us-east-1"
dynamodb_table = "terraform-state-lock"
encrypt        = true

